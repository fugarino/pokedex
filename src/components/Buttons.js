import React from "react";
import Button from "./Button";

const Buttons = () => {
  return (
    <div className="Buttons">
      <Button />
      <Button />
    </div>
  );
};

export default Buttons;
